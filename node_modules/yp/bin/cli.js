'use strict';
//  shim for Proxy
require('harmony-reflect');
const pss = require('../lib/pss');
const path = require('path');

const config = require(path.join(process.cwd(), 'pss'));

config.rootPath = config.rootPath ? path.join(process.cwd(), config.rootPath) : process.cwd();

pss(config);
