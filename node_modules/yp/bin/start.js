#!/usr/bin/env node

'use strict';
const spawn = require('child_process').spawn;
const path = require('path');

spawn('node', ['--harmony_proxies',
  '--harmony_default_parameters',
  path.join(__dirname, 'cli.js')], {
    stdio: 'inherit',
    cwd: process.cwd(),
  });

