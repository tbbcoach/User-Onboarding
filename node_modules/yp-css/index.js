/**
 * Created by aesop on 2016/2/1.
 */
'use strict';

const dir = './lib/';

module.exports = {
    'construct': require(dir + 'pre'),
    'dependency': require(dir + 'dependency'),
    'before:save': x => x,
    'insert-uri': require(dir + 'insert-uri'),
    'name': x =>'css'
}